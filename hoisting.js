var a ;
console.log(a); // undefined
 a = 5;
console.log(a); // 5

// declaration of function than console at last we declare the variable

// HOISTING CONCEPT
// THE code not throws error because of hoisting and the code runs without errors

// in hoisting the declaration of variable and function is moved to the top of their scope before code execution
// only declaration is hoisted not the initialization